# @kb/config

共享工程配置（私有包，不发布）：

- `@kb/config/tsconfig` — 严格模式 tsconfig 基线
- `@kb/config/eslint` — ESLint 9 flat config（js + ts + vue3-recommended + prettier）
- `@kb/config/prettier` — Prettier 配置（无分号、单引号、100 列）
- `@kb/config/stylelint` — stylelint 配置（含 `kb-` 语义化 class 命名校验）
